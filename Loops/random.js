// To generate random number between 0 and 1;
let x = Math.random()
// if i want integers then multiply it by the maximum value we need
// we can also set starting point by adding such as 1
let y = Math.floor((Math.random()*50)+1)
console.log(y);